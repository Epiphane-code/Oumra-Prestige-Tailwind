name: Test GitHub Actions

on:
  push:
    branches:
      - main

jobs:
  test-job:
    runs-on: ubuntu-latest

    steps:
      - name: Afficher un message
        run: echo "Hello Epiphane, GitHub Actions fonctionne 🚀"


